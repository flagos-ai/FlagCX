[build-system]
requires = ["setuptools", "wheel", "torch", "packaging", "setuptools_scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "flagcx"
dynamic = ["version"]
description = "FlagCX: A unified collective communication library"
requires-python = ">=3.8"
